# Web App (Vite)
# Copie para .env.local (dev) e configure também no Cloudflare Pages (Production).

# IPTV Proxy
VITE_IPTV_PROXY_URL=http://localhost:3101

# Optional same-origin API base (only used if you run a backend)
VITE_API_URL=/api

# Only try backend endpoints when explicitly enabled
VITE_TRY_BACKEND=false

# Supabase Auth (easiest global login)
# Crie um projeto em https://supabase.com e pegue em Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
